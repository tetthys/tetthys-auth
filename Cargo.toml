[package]
name = "tetthys-auth"
version = "0.2.0"
edition = "2021"

description = "Abtract authentication crate for rust web apps."
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/tetthys/tetthys-auth"
keywords = ["auth", "authentication"]
categories = ["auth", "authentication"]

[dependencies]

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
